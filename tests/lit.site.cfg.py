# Autogenerated from /Users/swapnaiyer/repos/mlir-experiment/tests/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
import os
import platform
def path(p):
    if not p: return ''
    # Follows lit.util.abs_path_preserve_drive, which cannot be imported here.
    if platform.system() == 'Windows':
        return os.path.abspath(os.path.join(os.path.dirname(__file__), p))
    else:
        return os.path.realpath(os.path.join(os.path.dirname(__file__), p))


import sys

config.project_binary_dir = "/Users/swapnaiyer/repos/build_mlir"
config.project_source_dir = "/Users/swapnaiyer/repos/mlir-experiment"
config.llvm_tools_dir = lit_config.substitute("/usr/local/Cellar/llvm/18.1.8/bin")
config.mlir_obj_root = ""
config.llvm_shlib_ext = ""

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Let the main config do the real work
lit_config.load_config(config, "/Users/swapnaiyer/repos/mlir-experiment/tests/lit.cfg.py")
